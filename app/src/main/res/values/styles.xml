<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="boton">
        <item name="android:background">@drawable/redondo</item>
    </style>
    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
    </style>

    <style name="StyledToast">

        <item name="android:textColor">#fff</item>
        <item name="android:textStyle"></item> only bold!
        <item name="android:fontFamily"></item> For custom fonts just add the path -> fonts/myfont.ttf
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="android:alpha">255</item>   value between 0-255 where 255 is full solid
        <item name="android:icon">@drawable/gas_station</item>  drawable id of the icon. R.drawable.xx

    </style>
    <style name="StyledToastError">

        <item name="android:textColor">#fff</item>
        <item name="android:textStyle"></item> only bold!
        <item name="android:fontFamily"></item> For custom fonts just add the path -> fonts/myfont.ttf
        <item name="android:colorBackground">#D32F2F</item>
        <item name="android:alpha">255</item>   value between 0-255 where 255 is full solid
        <item name="android:icon">@drawable/alert</item>  drawable id of the icon. R.drawable.xx

    </style>


</resources>
